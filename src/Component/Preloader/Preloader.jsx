import React from 'react';
import './Preloader.css'; // Ensure the correct CSS file is imported

const Preloader = () => {
  return (
    <div id="loader">
      {/* Loader animation goes here */}
    </div>
  );
};

export default Preloader;
